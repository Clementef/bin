#!/bin/sh
optlist=""
#compile directory list
while IFS= read -r line; do
    optlist="$(printf "%s\n%s" "${optlist}" "$(find "$line" -type f | grep "/*.pdf")")"
done < "$RES/.pdfdirs"
uinp="$(echo "$optlist" | dmenu -i -p "Pdf:")"

#open pdf with zathura
[ -n "$uinp" ] && zathura "$uinp"
